<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.16</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.jeysiva</groupId>
	<artifactId>booking-service</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>booking-service</name>
	<description>Booking service: a Trip = flight seat + hotel room, booked atomically</description>

	<properties>
		<java.version>17</java.version>
		<springdoc.version>2.8.17</springdoc.version>
		<spring-cloud.version>2025.0.3</spring-cloud.version>
	</properties>

	<!-- No shared parent across modules, so the Spring Cloud BOM is imported per-service. -->
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<!-- Register with discovery-service (Eureka) instead of being a fixed host:port. -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!-- JPA/Hibernate persistence. Brings in HikariCP (the default connection pool) transitively. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Kafka for the choreography saga. On Boot 3 the Kafka auto-configuration ships inside
		     spring-boot-autoconfigure, so spring-kafka is the only dependency needed (version managed by
		     spring-boot-starter-parent). -->
		<dependency>
			<groupId>org.springframework.kafka</groupId>
			<artifactId>spring-kafka</artifactId>
		</dependency>

		<!-- Swagger UI + OpenAPI 3 docs. springdoc 2.x is the line that supports Spring Boot 3. -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>${springdoc.version}</version>
		</dependency>

		<!-- Distributed tracing. On Boot 3 this is three pieces, not the five small modules Boot 4 splits it
		     into (spring-boot-opentelemetry / -micrometer-tracing / -micrometer-tracing-opentelemetry
		     / -zipkin do not exist on this line):
		       - spring-boot-starter-actuator      : brings in Micrometer + the Observation API auto-config
		       - micrometer-tracing-bridge-otel    : the bridge that creates a real Tracer backed by the
		                                             OpenTelemetry SDK. Without it Boot quietly falls back to a
		                                             no-op tracer — no error, just no spans.
		       - opentelemetry-exporter-zipkin     : reports finished spans to a Zipkin server
		     No explicit versions — all managed by the spring-boot-starter-parent BOM. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-tracing-bridge-otel</artifactId>
		</dependency>
		<dependency>
			<groupId>io.opentelemetry</groupId>
			<artifactId>opentelemetry-exporter-zipkin</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
